You are a senior full-stack software engineer, UI/UX designer, database architect and DevOps engineer.

I am attaching a PDF/PPT specification document for a project called "MargSarthi".

Your task is to BUILD THE COMPLETE, FULLY WORKING, PRODUCTION-READY WEBSITE described below.

IMPORTANT:
- Do NOT create only a frontend mockup.
- Do NOT use fake/static data for core functionality.
- Do NOT leave important buttons non-functional.
- Build the frontend, backend, database schema, authentication, APIs, validation, admin dashboard and deployment configuration.
- The application must actually work end-to-end.
- Use the attached PPT as a requirements/reference document.
- If there is any conflict between the PPT and this prompt, follow this prompt for architecture and role behavior, while preserving the functional requirements from the PPT.
- Do not blindly copy the visual design from the PPT. Improve it significantly.

==================================================
1. PROJECT OVERVIEW
==================================================

Project name:

MargSarthi

MargSarthi is an education consultancy platform where students can register, complete their profile, request consultancy/help, raise support tickets, request counselor calls, view updates/notices and track the status of their requests.

There are ONLY TWO ROLES for the current version:

1. STUDENT
2. ADMIN

The ADMIN acts as the system administrator and counselor/operator.

Do NOT create a separate counselor role at this stage.

The application should be designed so that a counselor role can be added later without rewriting the architecture.

==================================================
2. TECHNOLOGY STACK
==================================================

Use the following stack unless there is a very strong technical reason to change something:

FRONTEND:
- React
- Vite
- TypeScript
- Tailwind CSS
- React Router
- Axios
- TanStack Query / React Query where appropriate
- Modern component architecture
- Lucide icons or another clean icon library
- Responsive design

BACKEND:
- Node.js
- Express.js
- TypeScript
- REST API
- Prisma ORM

DATABASE:
- PostgreSQL

CURRENT DATABASE HOSTING:
- Railway PostgreSQL

CURRENT BACKEND HOSTING:
- Render

FRONTEND HOSTING:
- Vercel

DOMAIN:
- margsarthi.co.in

EXPECTED API DOMAIN:
- api.margsarthi.co.in

EMAIL:
- The project will eventually use:
  admin@margsarthi.co.in

Do not hardcode credentials, API keys, database passwords, SMTP passwords or payment keys anywhere in source code.

Everything sensitive must be environment variables.

==================================================
3. EXPECTED SCALE
==================================================

The application must be architected to comfortably support at least:

100–150 concurrent users during peak usage.

Design both the backend and database with this requirement in mind.

Do NOT claim that "100 users" means 100 requests/second.

Instead, design the system properly for concurrent users and traffic bursts.

BACKEND REQUIREMENTS:

- Stateless API architecture
- Efficient async I/O
- No blocking operations
- Proper database connection pooling
- Pagination for large queries
- Avoid N+1 database queries
- Proper indexes
- Efficient Prisma queries
- Input validation
- Rate limiting
- Request size limits
- CORS configuration
- Helmet/security headers
- Proper error handling
- Structured logging
- Health-check endpoint
- Graceful shutdown
- Environment-based configuration
- No unnecessary database calls
- No loading entire tables into memory
- No unbounded API responses

DATABASE REQUIREMENTS:

- PostgreSQL
- Prisma
- Foreign keys
- Proper normalization
- Appropriate indexes
- Unique constraints
- Transactions where required
- Connection pooling
- Avoid N+1 queries
- Efficient filtering/sorting
- Pagination
- Proper timestamps
- Soft-delete where appropriate
- Referential integrity

Design the database so it can later scale beyond the initial 100–150 concurrent users.

==================================================
4. USER ROLES
==================================================

ROLE 1: STUDENT

Students can:

- Register
- Login
- Logout
- View dashboard
- Complete/edit profile
- Receive a unique Student ID
- View Student ID
- Raise consultancy/support tickets
- Select ticket category
- Select priority
- Enter description
- See applicable amount
- Make payment where applicable
- View open/pending requests
- View closed/resolved requests
- View individual ticket details
- View admin responses/comments
- Request a Quick Call
- View available counselor/admin time slots
- Book a call slot
- Receive confirmation
- View booked calls
- View notices/updates
- Upload required documents
- View their uploaded documents
- View education details
- View account information
- Access contact/support information

Students MUST NOT be able to:

- Access admin dashboard
- See other students
- See other students' tickets
- Modify ticket status
- Modify payment status
- Create admin accounts
- Access admin APIs

==================================================
5. ADMIN ROLE
==================================================

There should be a dedicated Admin Dashboard.

Initial admin:

Email:
anmolkj006@gmail.com

IMPORTANT:

Seed/create this account as ADMIN.

Do NOT expose a public "register as admin" option.

Future admin email will be changed to:

admin@margsarthi.co.in

Therefore admin identity/configuration must be easy to change without rewriting the application.

The admin should be able to:

- Login
- View dashboard statistics
- View all student accounts
- Search students
- View individual student profiles
- View student documents
- View all open/pending tickets
- View closed tickets
- Search/filter tickets
- Open individual ticket
- View complete ticket details
- View student information associated with ticket
- Add an admin comment/response
- Change ticket status
- Close ticket
- Reopen ticket if required
- View ticket history
- View payments
- View Quick Call bookings
- Create/manage available call slots
- View booked calls
- Manage notices/updates
- Create notice
- Edit notice
- Delete/deactivate notice
- View uploaded documents
- View platform statistics

==================================================
6. TICKET / CONSULTANCY SYSTEM
==================================================

This is one of the CORE features.

Students should have a prominent:

"Raise a Request"

or

"Raise Ticket"

action.

The form should include:

CATEGORY:

Examples:
- Admissions
- Scholarships
- Internships
- Career Guidance
- College Selection
- Education
- Other

Make categories database-driven so they can be changed later.

PRIORITY:

At minimum:

- Low
- Medium
- High
- Urgent

The PPT specifically mentions priority tiers and amount fields.

The ticket form should contain:

- Category
- Priority
- Description
- Amount
- Optional attachment
- Created timestamp

If pricing is category/priority based, design the database so pricing can be configured rather than hardcoded in React.

The UI should show the applicable amount clearly before payment.

==================================================
7. TICKET STATUS SYSTEM
==================================================

Use clear statuses.

Recommended:

OPEN
IN_PROGRESS
WAITING_FOR_STUDENT
RESOLVED
CLOSED

The student dashboard should primarily show:

OPEN / PENDING REQUESTS

When an admin closes/resolves a request:

It must disappear from the student's active/pending list.

It should appear in:

"Closed Requests"

Example:

ACTIVE REQUESTS:

#MS-2026-00142
Admission Guidance
High Priority
Status: In Progress

#MS-2026-00143
Scholarship Help
Medium Priority
Status: Open


CLOSED REQUESTS:

#MS-2026-00120
College Selection
Status: Closed

The same behavior must exist in the Admin dashboard:

OPEN/PENDING REQUESTS

and

CLOSED REQUESTS

When admin closes a request, it should no longer appear in the default open/pending queue.

==================================================
8. ADMIN TICKET WORKFLOW
==================================================

Admin opens:

Tickets → Open Requests

Admin sees:

- Ticket ID
- Student name
- Student ID
- Category
- Priority
- Amount
- Created time
- Current status

Admin clicks a ticket.

Detailed view:

--------------------------------
Ticket #MS-2026-00142

Student:
Anmol Kumar

Student ID:
MS-2026-00142

Category:
Admissions

Priority:
High

Description:
Student's issue...

Payment:
Paid

Created:
...

Status:
IN_PROGRESS
--------------------------------

Admin can write:

"Admin response/comment"

Then:

[Save Response]

Admin can change status:

OPEN
IN_PROGRESS
WAITING_FOR_STUDENT
RESOLVED
CLOSED

If admin chooses CLOSE:

Show confirmation.

Optional/required closing note:

"Closing comment"

Example:

"Your admission query has been resolved. Please contact us again if you need further assistance."

After closing:

- ticket.status = CLOSED
- closedAt = current timestamp
- closedBy = admin ID
- closing comment stored
- ticket removed from active queue
- ticket appears in closed tickets
- student sees it in Closed Requests

Maintain a ticket status/history table so changes can be audited.

==================================================
9. STUDENT ID GENERATION
==================================================

This is specifically mentioned in the PPT.

Automatically generate a unique Student ID after successful onboarding/registration.

Example:

MS-2026-00142

Requirements:

- Must be unique
- Must be generated server-side
- Never generated by frontend
- Must not collide under concurrent registrations
- Use database transaction/sequence-safe mechanism
- Display prominently on student profile/dashboard

Do NOT generate IDs using simple random frontend logic.

==================================================
10. STUDENT REGISTRATION
==================================================

Registration fields:

- Full Name
- Email
- Phone Number
- Password
- Confirm Password

Validate:

- Email format
- Phone number
- Password strength
- Duplicate email
- Duplicate phone if required
- Required fields

Password MUST be hashed.

Never store plain-text passwords.

Use bcrypt or Argon2.

After registration:

- Create user
- Assign STUDENT role
- Create student profile
- Generate Student ID
- Redirect to onboarding/profile completion
- Show success message

==================================================
11. AUTHENTICATION
==================================================

Implement real authentication.

Use:

JWT-based authentication OR secure session-based authentication.

If using JWT:

- Short-lived access token
- Refresh token strategy if needed
- Secure storage
- Avoid storing sensitive tokens insecurely
- Middleware for authentication
- Middleware for role authorization

Create middleware such as:

requireAuth
requireStudent
requireAdmin

Backend MUST enforce authorization.

Never rely only on frontend route protection.

==================================================
12. STUDENT PROFILE
==================================================

Profile should include:

PERSONAL:

- Full Name
- Email
- Phone
- Date of Birth if required
- Gender if required
- Address

CASTE:

The PPT specifically mentions adding caste information and certificate upload.

Include:

- Caste category
- Certificate upload
- Certificate metadata

Do not expose certificate files publicly.

EDUCATION:

Include:

- School/College
- College name
- Course
- Stream
- Session
- Semester
- Graduation year
- Migration/CLC information
- Other relevant education fields

The PPT specifically identifies missing:

- College list
- Migration/CLC
- Session
- Semester
- Stream

Implement these properly.

==================================================
13. DOCUMENT UPLOADS
==================================================

Students should be able to upload documents such as:

- Caste certificate
- Migration certificate
- CLC
- Other required documents

IMPORTANT:

Do NOT store large PDFs/images directly inside PostgreSQL.

Store files in object storage.

The database should store:

- document ID
- student ID
- document type
- storage key/path
- original filename
- MIME type
- file size
- upload timestamp

Files must NOT be publicly accessible.

Use signed/private URLs or protected backend endpoints.

Structure the code so object storage provider can easily be changed later.

==================================================
14. QUICK CALL
==================================================

The PPT specifically says Quick Call is currently inactive and needs to be activated.

Implement it.

Student:

Quick Call
↓
See available slots
↓
Choose date
↓
Choose time
↓
Book slot
↓
Confirmation

Admin:

Quick Call Management
↓
Create slot
↓
Set date
↓
Set start time
↓
Set end time
↓
Available capacity
↓
View bookings

Prevent:

- Double booking
- Booking unavailable slots
- Booking the same slot multiple times
- Booking past slots

Use database transactions/constraints where necessary.

After booking:

Student sees:

Appointment Confirmed

Date:
...

Time:
...

Status:
CONFIRMED

Design the email notification architecture so email/SMS confirmation can be added through environment variables/provider configuration.

==================================================
15. PAYMENT
==================================================

The PPT describes:

Raise Ticket
→ Preview & Pay
→ Guidance

and mentions ticket pricing up to approximately ₹50.

Implement a payment-ready architecture.

IMPORTANT:

Do not hardcode payment secrets.

Create environment variables such as:

PAYMENT_KEY_ID
PAYMENT_KEY_SECRET
PAYMENT_WEBHOOK_SECRET

Use a payment provider abstraction so the provider can be configured later.

Payment state must be stored server-side.

Possible states:

PENDING
SUCCESS
FAILED
REFUNDED

Do not trust frontend payment success.

Payment verification must happen on the backend.

If payment provider credentials are not available during local development, provide a development/mock payment mode controlled by an environment variable.

Example:

PAYMENT_MODE=mock

But production must use real verification.

==================================================
16. NOTICES / UPDATES
==================================================

The PPT says the Notice Board is currently empty and should be populated.

Implement:

Student:

Updates / Notices

Admin:

Manage Notices

Admin can:

- Create
- Edit
- Publish/unpublish
- Delete/archive

Notice fields:

- Title
- Content
- Category
- Published date
- Expiry date
- Author
- Status

Students see published notices only.

Create a clean announcement UI.

==================================================
17. HOMEPAGE
==================================================

Create a beautiful landing page.

Navigation:

- Home
- About
- How It Works
- Services
- Updates
- Quick Call
- Contact
- Login
- Register
- Get Started

The PPT references these navigation elements.

Homepage should clearly communicate:

MargSarthi
"India's Trusted Education Guidance Platform"

Purpose:

Connect students with expert guidance for:

- Admissions
- Scholarships
- Internships
- Career guidance

Use a strong hero section.

Include:

- CTA
- How it works
- Services
- Trust indicators
- College network section
- FAQ
- Contact section
- Footer

Do NOT blindly use "50K+ students" or "95% success rate" unless represented as configurable content.

The PPT itself says these figures need verification.

Make statistics configurable.

==================================================
18. DESIGN / UI REQUIREMENTS
==================================================

The UI is VERY IMPORTANT.

I want:

CLEAN
ELEGANT
MINIMALISTIC
MODERN
PREMIUM
SLIGHTLY CREATIVE

Do NOT make it look like a generic Bootstrap admin dashboard.

Do NOT make it overly colorful.

Do NOT make every section card-shaped.

Do NOT use excessive gradients.

Do NOT use huge animations.

Design inspiration:

- Modern SaaS
- Premium education platform
- Clean fintech-like dashboard
- Minimal typography
- Strong whitespace
- Excellent hierarchy

Use subtle creativity through:

- tasteful gradients
- soft background shapes
- subtle hover effects
- micro-interactions
- smooth transitions
- elegant cards
- subtle glass effects only where appropriate
- clean iconography
- tasteful illustrations

The application should feel like a real startup product.

Use a consistent design system.

Define:

- colors
- typography
- spacing
- border radius
- shadows
- buttons
- inputs
- cards
- tables
- modals
- alerts
- badges

==================================================
19. STUDENT DASHBOARD
==================================================

Create a polished student dashboard.

Example:

-------------------------------------
Good morning, Anmol 👋

Student ID
MS-2026-00142

-------------------------------------

Quick actions:

[Raise Request]
[Quick Call]
[Update Profile]

-------------------------------------

Active Requests

Ticket #MS-2026-00142
Admissions
High
In Progress

-------------------------------------

Upcoming Call

18 September
4:30 PM

-------------------------------------

Latest Updates

Admission deadline...
Scholarship announcement...

-------------------------------------

Profile Completion
85%

-------------------------------------

Make the dashboard responsive.

Mobile view is extremely important.

==================================================
20. ADMIN DASHBOARD
==================================================

Admin dashboard should feel like a professional operations console.

Top statistics:

- Total Students
- Open Requests
- In Progress
- Closed Requests
- Today's Calls
- Revenue if applicable

Main navigation:

Dashboard
Students
Open Requests
Closed Requests
Quick Calls
Notices
Payments
Documents
Settings

Default ticket view should show ACTIVE requests.

Admin should NOT see closed tickets in the default active list.

Have a dedicated:

"Closed Requests"

section.

Add:

- Search
- Filters
- Priority filters
- Category filters
- Status filters
- Date filters
- Pagination

==================================================
21. ADMIN STUDENT MANAGEMENT
==================================================

Admin should be able to:

View all students.

Search by:

- Name
- Email
- Phone
- Student ID

Open student profile.

View:

- Personal details
- Education
- Documents
- Tickets
- Payments
- Calls

Do not allow admin to accidentally expose sensitive documents publicly.

==================================================
22. API DESIGN
==================================================

Use clean REST APIs.

Example structure:

/api/auth/register
/api/auth/login
/api/auth/logout
/api/auth/me

/api/students/profile
/api/students/documents

/api/tickets
/api/tickets/:id
/api/tickets/:id/messages
/api/tickets/:id/status
/api/tickets/:id/close

/api/appointments
/api/appointments/slots
/api/appointments/:id

/api/notices
/api/notices/:id

/api/payments
/api/payments/:id
/api/payments/webhook

/api/admin/dashboard
/api/admin/students
/api/admin/tickets
/api/admin/appointments
/api/admin/notices
/api/admin/payments
/api/admin/documents

Keep route/controller/service/database responsibilities separated.

Recommended backend architecture:

src/
  config/
  controllers/
  routes/
  services/
  middleware/
  validators/
  utils/
  types/
  prisma/
  jobs/
  app.ts
  server.ts

Do not put all logic in route files.

==================================================
23. DATABASE DESIGN
==================================================

Design a proper relational PostgreSQL schema.

At minimum consider:

User
StudentProfile
Education
Document
Ticket
TicketMessage
TicketStatusHistory
TicketCategory
TicketPriority
Payment
AppointmentSlot
Appointment
Notice
Notification

Use:

- UUIDs where appropriate
- unique constraints
- foreign keys
- createdAt
- updatedAt
- indexes

Important indexes:

User.email
User.phone

StudentProfile.studentId

Ticket.studentId
Ticket.status
Ticket.priority
Ticket.createdAt

Payment.ticketId
Payment.status

Appointment.studentId
Appointment.slotId

Notice.status
Notice.publishedAt

Use compound indexes where they make sense.

==================================================
24. CONCURRENCY / DATABASE PERFORMANCE
==================================================

This is mandatory.

Design for 100–150 concurrent users.

For example:

- Use PostgreSQL connection pooling
- Keep transactions short
- Avoid long-running transactions
- Use indexes
- Paginate admin lists
- Never fetch all tickets/students at once
- Use select/include carefully in Prisma
- Avoid N+1 queries
- Use atomic updates where appropriate
- Use database constraints to prevent duplicate bookings
- Use transactions for payment/ticket operations
- Use transactions for Student ID generation
- Use appropriate isolation where necessary

Do not create unnecessary polling.

If polling is needed, use reasonable intervals.

Keep the API stateless so multiple backend instances can be added later.

==================================================
25. SECURITY
==================================================

Implement:

- Helmet
- CORS
- Rate limiting
- Input validation
- Password hashing
- Authentication middleware
- Role authorization
- Secure cookies/token handling
- SQL injection protection through Prisma
- XSS-safe output handling
- Request body size limits
- File upload restrictions
- MIME validation
- File size limits
- Protected document access
- No secrets in Git
- Environment variables
- Production error sanitization
- Development vs production config

Never return passwords or sensitive authentication data.

Never trust:

- role from frontend
- payment status from frontend
- student ID from frontend
- ticket owner ID from frontend

Always derive identity from authenticated backend context.

==================================================
26. EMAIL SYSTEM
==================================================

Create a reusable email service.

Possible emails:

Student registration
Password reset
Ticket created
Admin response
Ticket resolved
Ticket closed
Quick Call booked
Payment successful

Do not hardcode SMTP credentials.

Environment variables:

SMTP_HOST
SMTP_PORT
SMTP_USER
SMTP_PASSWORD
SMTP_FROM

For production, FROM can eventually be:

admin@margsarthi.co.in

The application should continue working even if email configuration is temporarily unavailable in development.

==================================================
27. ERROR HANDLING
==================================================

Create consistent API responses.

Example:

{
  "success": false,
  "message": "Ticket not found"
}

Handle:

400
401
403
404
409
422
429
500

Create a global Express error handler.

Never expose stack traces in production.

==================================================
28. FRONTEND STATE MANAGEMENT
==================================================

Use a clean approach.

React Query/TanStack Query can handle:

- API caching
- loading
- errors
- refetching
- mutations

Avoid putting everything into global state.

Use local state where appropriate.

Create reusable components:

Button
Input
Select
Modal
Table
Badge
Toast
Loader
EmptyState
Pagination
ConfirmDialog
FileUploader
StatusBadge
PriorityBadge

==================================================
29. RESPONSIVENESS
==================================================

The website MUST work on:

- Desktop
- Laptop
- Tablet
- Mobile

Admin dashboard should be usable on tablets/mobile.

Student interface should be excellent on mobile.

Do not simply shrink desktop UI.

Create proper responsive layouts.

==================================================
30. ACCESSIBILITY
==================================================

Implement basic accessibility:

- semantic HTML
- labels
- keyboard navigation
- visible focus states
- accessible buttons
- accessible dialogs
- appropriate contrast
- alt text
- error messages connected to inputs

==================================================
31. SEO
==================================================

For public pages:

- proper title
- meta description
- Open Graph metadata
- semantic headings
- favicon
- robots configuration
- sitemap where applicable

Use:

margsarthi.co.in

as the production domain.

==================================================
32. ROUTING
==================================================

Public:

/
/about
/services
/how-it-works
/updates
/contact
/login
/register

Student:

/student/dashboard
/student/profile
/student/tickets
/student/tickets/:id
/student/closed-requests
/student/quick-call
/student/appointments
/student/updates

Admin:

/admin
/admin/students
/admin/students/:id
/admin/tickets
/admin/tickets/:id
/admin/closed-requests
/admin/quick-calls
/admin/notices
/admin/payments
/admin/documents
/admin/settings

Protect routes based on role.

==================================================
33. SEED DATA
==================================================

Create Prisma seed functionality.

Seed:

Admin:

Email:
anmolkj006@gmail.com

Role:
ADMIN

Do not hardcode the admin password into the repository.

Use an environment variable:

INITIAL_ADMIN_PASSWORD

or provide a CLI/setup mechanism.

Seed a few development-only:

- ticket categories
- priorities
- sample notices
- sample appointment slots

Make sample data clearly development-only.

Do not seed fake production student data.

==================================================
34. ENVIRONMENT VARIABLES
==================================================

Create:

.env.example

Example:

DATABASE_URL=

JWT_SECRET=
JWT_REFRESH_SECRET=

FRONTEND_URL=
API_URL=

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

PAYMENT_MODE=
PAYMENT_KEY_ID=
PAYMENT_KEY_SECRET=
PAYMENT_WEBHOOK_SECRET=

STORAGE_ENDPOINT=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
STORAGE_BUCKET=

INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=

Never commit .env.

Add appropriate .gitignore.

==================================================
35. DEPLOYMENT
==================================================

The target deployment is:

FRONTEND:
Vercel

BACKEND:
Render

DATABASE:
Railway PostgreSQL

DOMAIN:
GoDaddy

Production:

https://margsarthi.co.in

API:

https://api.margsarthi.co.in

The backend must be configured to accept requests from the production frontend domain.

CORS:

FRONTEND_URL=https://margsarthi.co.in

The backend should also have:

GET /health

which returns something like:

{
  "status": "ok"
}

Use environment variables for all deployment-specific configuration.

==================================================
36. RAILWAY POSTGRESQL
==================================================

The database will initially be deployed on Railway.

Make the application compatible with Railway PostgreSQL.

DATABASE_URL must come from the Railway environment.

Prisma migration commands should work in deployment.

Provide:

prisma/schema.prisma

prisma/seed.ts

migration workflow.

Do not depend on local database behavior.

==================================================
37. RENDER BACKEND
==================================================

Backend will be deployed on Render.

Prepare:

package.json
tsconfig.json
Dockerfile if useful
render.yaml if useful

Use a production start command.

Example:

npm run build
npm start

Make sure Prisma generation happens correctly.

If using migrations:

npx prisma migrate deploy

Do not use:

prisma migrate dev

in production.

==================================================
38. VERCEL FRONTEND
==================================================

Frontend should be ready for Vercel deployment.

Use:

VITE_API_URL

for the API.

Do not hardcode:

localhost:5000

into the application.

Development:

VITE_API_URL=http://localhost:5000

Production:

VITE_API_URL=https://api.margsarthi.co.in

==================================================
39. CUSTOM DOMAIN
==================================================

The final setup will be:

margsarthi.co.in
→ Vercel

api.margsarthi.co.in
→ Render

Email:

admin@margsarthi.co.in

Do not hardcode DNS values because they will be supplied by Vercel/Render.

Create deployment documentation explaining exactly where each DNS record should be added.

==================================================
40. PROJECT STRUCTURE
==================================================

Prefer a clean monorepo or clearly separated structure.

For example:

margsarthi/
│
├── frontend/
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── ...
│
├── backend/
│   ├── src/
│   ├── prisma/
│   ├── package.json
│   └── ...
│
├── README.md
└── .gitignore

==================================================
41. TESTING
==================================================

Implement basic tests.

Backend:

- registration
- login
- authentication
- authorization
- ticket creation
- ticket closing
- student cannot access admin API
- admin can access admin API
- duplicate email handling
- ticket ownership
- appointment booking
- duplicate appointment prevention

Frontend:

At minimum ensure critical flows work.

==================================================
42. LOADING / EMPTY / ERROR STATES
==================================================

Every major page must handle:

Loading
Error
Empty
Success

Examples:

"No open requests"

"No closed requests"

"No appointment slots available"

"No notices available"

"Failed to load requests"

Do not leave blank screens.

==================================================
43. DO NOT CREATE FAKE FUNCTIONALITY
==================================================

This is extremely important.

If a button exists:

It must work.

Examples:

"Raise Ticket"
→ actually creates ticket

"Close Request"
→ actually changes DB status

"Book Call"
→ actually creates appointment

"Create Notice"
→ actually stores notice

"Upload Document"
→ actually uploads file

"Pay"
→ actually starts payment flow or development mock flow

"Logout"
→ actually logs out

"Register"
→ actually creates account

Do not implement fake alerts such as:

"Coming soon"

for features that are supposed to be part of this version.

==================================================
44. PPT REQUIREMENTS TO INCORPORATE
==================================================

Read the attached PPT carefully and incorporate its identified pending issues.

Specifically make sure the new implementation addresses:

1. MargSarthi branding everywhere

2. Student ID generation

3. Quick Call with real available slots

4. Raise Ticket:
   - priorities
   - amount
   - description

5. Notice Board / Updates

6. T&C/PDF access

7. Caste field

8. Certificate upload

9. Education fields:
   - college
   - session
   - semester
   - stream
   - Migration/CLC

10. Payment flow

11. Student profile

12. Consultant/counselor guidance workflow

The PPT also contains future roadmap items such as:

- AI college/scholarship matching
- live chat
- regional languages
- expanding college partnerships

Architect the application so these can be added later, but do NOT allow future features to make the current application unnecessarily complicated.

For example:

Create service boundaries/interfaces where future AI matching can be plugged in later.

==================================================
45. FUTURE-PROOF ARCHITECTURE
==================================================

The system should later support:

- Counselor role
- AI college matching
- AI scholarship matching
- Live chat
- Notifications
- SMS
- regional languages
- more payment providers
- more document types
- multiple admins
- multiple counselors

But DO NOT overengineer.

Build a clean modular monolith first.

Do NOT introduce microservices.

Do NOT introduce Kubernetes.

Do NOT introduce unnecessary Redis/Kafka/etc.

The initial architecture should be:

React
+
Node/Express
+
Prisma
+
PostgreSQL

==================================================
46. PERFORMANCE
==================================================

Optimize for:

Fast initial page load
Fast API responses
Efficient DB queries
Pagination
Lazy loading where appropriate
Code splitting
Image optimization
Minimal bundle size

Do not make the dashboard fetch everything at once.

For example:

Admin should NOT request:

100,000 students

just to display the first 20.

Use:

?page=1&limit=20

etc.

==================================================
47. LOGGING
==================================================

Implement structured backend logging.

Log:

- request method
- endpoint
- response status
- duration
- important errors

Do NOT log:

- passwords
- JWT secrets
- database passwords
- payment secrets
- private documents
- sensitive student data unnecessarily

==================================================
48. DOCUMENTATION
==================================================

Create a complete README.md.

It must explain:

1. Project overview
2. Features
3. Tech stack
4. Architecture
5. Folder structure
6. Local setup
7. Environment variables
8. Database setup
9. Prisma setup
10. Seed admin
11. Running frontend
12. Running backend
13. Running tests
14. Railway PostgreSQL deployment
15. Render backend deployment
16. Vercel frontend deployment
17. Domain configuration
18. CORS configuration
19. Production checklist
20. Troubleshooting

==================================================
49. DEVELOPMENT WORKFLOW
==================================================

Do not try to write the entire application blindly without checking the requirements.

First:

1. Read the attached PPT.
2. Extract all functional requirements.
3. Identify entities.
4. Design database schema.
5. Design API.
6. Design frontend routes.
7. Design UI system.
8. Implement backend.
9. Implement database.
10. Implement frontend.
11. Integrate frontend/backend.
12. Add seed data.
13. Test critical workflows.
14. Fix errors.
15. Verify production build.
16. Provide deployment instructions.

==================================================
50. FINAL QUALITY BAR
==================================================

The final result should feel like an actual production startup website.

It should NOT feel like:

- a college project
- a template
- a generic CRUD dashboard
- a UI-only prototype
- an unfinished MVP

It should feel like a polished product that could actually be shown to students and administrators.

Prioritize:

1. Correct functionality
2. Security
3. Database integrity
4. Performance
5. Clean architecture
6. Excellent UX
7. Visual polish

==================================================
51. IMPORTANT FINAL INSTRUCTION
==================================================

DO NOT stop after creating the frontend.

DO NOT stop after creating the database.

DO NOT stop after creating the API.

Everything must be connected.

I want:

Frontend
↓
API
↓
Database

to work end-to-end.

For example:

Student registers
↓
Backend validates
↓
PostgreSQL creates user
↓
Student ID generated
↓
Student logs in
↓
Student completes profile
↓
Student raises ticket
↓
Payment is created/verified
↓
Ticket appears in Admin dashboard
↓
Admin responds
↓
Admin closes ticket
↓
Ticket disappears from Active Requests
↓
Ticket appears in Closed Requests
↓
Student sees the response and closed ticket

This complete flow must work.

==================================================
52. OUTPUT EXPECTATION
==================================================

After implementation, provide:

1. Complete source code
2. Complete folder structure
3. Database schema
4. Prisma migrations
5. Seed script
6. API documentation
7. .env.example
8. README
9. Deployment instructions
10. Test instructions
11. Production checklist

If you encounter missing API keys or external credentials:

DO NOT hardcode fake credentials.

Use environment variables and create a working development/mock implementation where appropriate.

I will provide the actual production API keys and environment variables later.

==================================================
START NOW
==================================================

First inspect the attached MargSarthi PPT thoroughly.

Extract the requirements from it.

Then plan the architecture.

Then implement the complete application.

Do not ask me to manually create dozens of files unless absolutely necessary.

Do not leave TODOs for core functionality.

Build the actual working application.